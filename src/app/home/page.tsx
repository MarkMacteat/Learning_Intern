'use client'

import {Component, FC, ReactNode } from 'react'

class HomePage extends Component {
    render() {
        return(
            <div>HomePage</div>
        )
    }
}

export default HomePage